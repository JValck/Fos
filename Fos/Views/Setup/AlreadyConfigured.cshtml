
@{
    ViewData["Title"] = Localizer["Al geconfigureerd"];
}

<div class="container">
    <div class="card border-danger">
        <div class="card-header">
            @ViewData["Title"]
        </div>
        <div class="card-body">
            <p class="card-text"><i class="fas fa-exclamation-triangle"></i> @Localizer["FOS is al geconfigureerd"]</p>
        </div>
    </div>
</div>
